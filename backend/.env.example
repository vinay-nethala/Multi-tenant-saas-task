
# Server
PORT=5000

# Postgres (single DB shared schema)
PGHOST=postgres
PGUSER=postgres
PGPASSWORD=postgres
PGDATABASE=saas_db
PGPORT=5432
# Optional full connection string
# DATABASE_URL=postgresql://postgres:postgres@postgres:5432/saas_db

# JWT
JWT_SECRET=replace_with_strong_secret
JWT_EXPIRES_IN=24h

# Frontend
FRONTEND_URL=http://localhost:3000

# Optional (for production)
# NODE_ENV=production
